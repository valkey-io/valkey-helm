name: Test valkey-operator chart

on:
  pull_request:
    paths:
      - "valkey-operator/**"
      - ".github/workflows/unittest-operator.yml"
  push:
    branches:
      - main
    paths:
      - "valkey-operator/**"
      - ".github/workflows/unittest-operator.yml"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.19.4

      - name: Install helm-unittest plugin
        run: helm plugin install https://github.com/helm-unittest/helm-unittest --version 1.0.2

      - name: Lint chart
        run: helm lint ./valkey-operator

      - name: Run unit tests
        run: helm unittest ./valkey-operator
