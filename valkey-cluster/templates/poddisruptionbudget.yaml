{{- if or .Values.podDisruptionBudget .Values.pdb.create }}
apiVersion: {{ include "valkey.capabilities.policy.apiVersion" . }}
kind: PodDisruptionBudget
metadata:
  name: {{ template "valkey.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  {{- /* FIX: Pass the top-level context '.' directly */}}
  labels: {{- include "valkey.labels" . | nindent 4 }}
  {{- if .Values.valkeyAnnotations }}
  annotations: {{- include "valkey.tplvalues.render" ( dict "value" .Values.valkeyAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.podDisruptionBudget }}
  {{- include "valkey.tplvalues.render" (dict "value" .Values.podDisruptionBudget "context" $) | nindent 2 }}
  {{- else }}
  {{- if .Values.pdb.minAvailable }}
  minAvailable: {{ .Values.pdb.minAvailable }}
  {{- end }}
  {{- if or .Values.pdb.maxUnavailable (not .Values.pdb.minAvailable) }}
  maxUnavailable: {{ .Values.pdb.maxUnavailable | default 1 }}
  {{- end  }}
  selector:
    {{- /* FIX: Pass the top-level context '.' directly */}}
    matchLabels: {{- include "valkey.selectorLabels" . | nindent 6 }}
    matchExpressions:
      - {key: job-name, operator: NotIn, values: [{{ template "valkey.fullname" . }}-cluster-update]}
  {{- end }}
{{- end }}